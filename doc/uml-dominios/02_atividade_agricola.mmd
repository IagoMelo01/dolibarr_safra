classDiagram
    direction TB

    namespace Atividade_Agricola {
        class FvActivity {
            +STATUS_DRAFT
            +STATUS_IN_PROGRESS
            +STATUS_COMPLETED
            +STATUS_CANCELED
            +rowid
            +ref
            +label
            +type
            +status
            +fk_project
            +fk_task
            +fk_fieldplot
            +area_total
            +create(user) int
            +fetch(id, ref) int
            +update(user) int
            +delete(user, deleteLinkedTask) int
            +start(user) int
            +complete(user) int
            +cancel(user, allowDelete) int
            +fetchLines() int
            +replaceInputLines(lines, user, tx) int
        }

        class FvActivityLine {
            +rowid
            +fk_activity
            +fk_product
            +qty_done
            +dose_done
            +fk_warehouse
            +fk_stock_movement
            +create(user) int
            +deleteForActivity(db, activityId) int
        }
    }

    class CommonObject
    class CommonObjectLine
    class User
    class Project
    class Task
    class Talhao

    CommonObject <|-- FvActivity
    CommonObjectLine <|-- FvActivityLine

    FvActivity "1" o-- "0..*" FvActivityLine : insumos/apontamentos
    FvActivity ..> User : creator/modifier
    FvActivity ..> Project : optional context
    FvActivity ..> Task : optional extrafield link
    FvActivity ..> Talhao : fk_fieldplot
